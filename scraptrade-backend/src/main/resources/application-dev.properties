# Local development profile — activate with: SPRING_PROFILES_ACTIVE=dev
spring.config.import=optional:file:./application-local.properties
spring.jpa.hibernate.ddl-auto=update
# expose-token + mail: configure in application-local.properties (true = dev token popup, false = SendGrid email)
# payment.provider: set in application-local.properties (simulated default, or paystack/momo)
